#ifndef VECTOR_H
#define VECTOR_H

class Vector {
public:
	float x,y,z;
	Vector(float,float,float);
	Vector operator+(Vector);
	Vector operator-(Vector);
	Vector operator*(float);
	Vector operator/(float);
	Vector norm();
};

#endif