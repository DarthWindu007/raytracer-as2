#ifndef RAY_H
#define RAY_H
#include "vector.h"
#include "point.h"
class Ray {
public:
    Point pos;
    Vector dir;
    float t_min;
    float t_max;
	Ray(Point , Vector , float ,float );

};

#endif
